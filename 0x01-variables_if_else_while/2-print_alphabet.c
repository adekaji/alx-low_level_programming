#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success/correct)
*/

int main(void)
{
	char alph;
	for (alph = 'a'; alph <= 'z'; alph++)
	{
		putchar(alph);
	}
	purchar('\n');
return (0)
}
